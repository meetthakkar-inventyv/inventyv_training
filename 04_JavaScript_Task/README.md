# Javascript Task

This folder contains JavaScript Practice tasks in the files task1.js and task3.js.

## Task 1: Arrays with Promises

**Description**
- Work with arrays
- The first element is removed and reused in a new array
- two arrays are merged together
- A promise is used to calculate sum of the elements and resolved or reject based on the condition


## Task 3: Symbols with Promises

**Description**
- Uses `Symbol` values instead of normal numbers
- Extract numeric values from symbol for calculation
- Build new Symbol-based arrays by combining multiple sources.
- Uses a Promise to evaluate the total sum and resolve or reject based on the condition